services:
  weight-tool:
    build: .
    container_name: weight-tool-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - weight-tool-net

networks:
  weight-tool-net:
    driver: bridge
